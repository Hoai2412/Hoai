<UserControl x:Class="QuanLySinhVien.QuanLyNguoiDung"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F9FAFB">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TIÊU ĐỀ -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="AccountCogOutline"
                                     Foreground="#1E3A8A"
                                     Width="30" Height="30"
                                     Margin="0,0,10,0"/>
            <TextBlock Text="QUẢN LÝ NGƯỜI DÙNG"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#1E3A8A"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- KHUNG CHÍNH -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="25"
                BorderBrush="#E2E8F0"
                BorderThickness="1">

            <StackPanel>

                <!-- FORM THÔNG TIN NGƯỜI DÙNG -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Tên đăng nhập:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtTenDangNhap" Height="30" Padding="5" BorderBrush="#CBD5E1" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Mật khẩu:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="txtMatKhau" Height="30" Padding="5" BorderBrush="#CBD5E1" BorderThickness="1"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Họ tên:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtHoTen" Height="30" Padding="5" BorderBrush="#CBD5E1" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Vai trò:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cbVaiTro" Height="30">
                            <ComboBoxItem Content="Sinh viên"/>
                            <ComboBoxItem Content="Giảng viên"/>
                            <ComboBoxItem Content="Quản trị viên"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- DANH SÁCH NGƯỜI DÙNG -->
                <DataGrid x:Name="dgNguoiDung"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          AlternatingRowBackground="#F1F5F9"
                          RowHeight="38"
                          GridLinesVisibility="Horizontal"
                          BorderBrush="#E2E8F0"
                          BorderThickness="1"
                          Background="White"
                          Margin="0,0,0,15"
                          FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding TenDangNhap}" Width="2*"/>
                        <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="2*"/>
                        <DataGridTextColumn Header="Vai trò" Binding="{Binding VaiTro}" Width="1.5*"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- NÚT CHỨC NĂNG -->
                <UniformGrid Columns="4" HorizontalAlignment="Center">
                    <Button Content="Thêm" Width="150" Height="38"
                            Background="#2563EB" Foreground="White" FontWeight="Bold"
                            Margin="5" Click="BtnThem_Click"/>

                    <Button Content="Sửa" Width="150" Height="38"
                            Background="#F59E0B" Foreground="White" FontWeight="Bold"
                            Margin="5" Click="BtnSua_Click"/>

                    <Button Content="Xóa" Width="150" Height="38"
                            Background="#DC2626" Foreground="White" FontWeight="Bold"
                            Margin="5" Click="BtnXoa_Click"/>

                    <Button Content="Khóa / Mở khóa" Width="150" Height="38"
                            Background="#16A34A" Foreground="White" FontWeight="Bold"
                            Margin="5" Click="BtnKhoa_Click"/>
                </UniformGrid>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
